<h1>Results for "<%= movieSearch %>":</h1>

<% parsedData['Search'].forEach(function(movie){ %>
    <li> 
        <em><%= movie['Title'] %></em> - <%= movie['Year'] %> 
    </li>
<% }); %>

<a href="/"> Search again </a>